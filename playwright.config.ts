import { defineConfig, devices } from '@playwright/test';

// Phase 6 of docs/internal/TESTING.md: end-to-end smoke coverage against a
// real browser. Kept separate from vitest.config.ts (different runner,
// different lifecycle — this one boots an actual dev server).
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: 'list',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 30_000,
  },
});
