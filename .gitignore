# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files — real keys must never be committed (.env.example is the template)
.env
.env.*
!.env.example

# Playwright (Phase 6 e2e)
/test-results/
/playwright-report/
/playwright/.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# References folder (internal use only)
REFERENCES/

# Claude Code local permissions (per-machine, not shared)
.claude/settings.local.json

# Demo recordings (scripts/record-demo.mjs output)
/recordings/
__pycache__/
